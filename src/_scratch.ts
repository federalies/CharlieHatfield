// import fs from 'fs'
// import path from 'path'

// const fsPromises = fs.promises

const tests = ['./abc/', '/abc/', '.abc/', 'abc/', 'abc']

  // tests.map(testData => {
  //   console.log(testData, trimPathstoToken(testData))
  // })

  //
;(async () => {
  // const wPath = '/Users/ericmoore/Web/Federalies/projects/codeGenSquals/codeGen/lambda'
  // const firstLower = (s: string) => {
  //   return `${s.slice(0, 1).toLowerCase()}${s.slice(1)}`
  // }
  // console.log(trimPathstoToken('../../eric/'))
  // console.log(trimPathstoToken('./././eric..///'))
})()
